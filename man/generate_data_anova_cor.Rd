% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{generate_data_anova_cor}
\alias{generate_data_anova_cor}
\title{Generate Correlated Data for Two-Group ANOVA}
\usage{
generate_data_anova_cor(M, S, Sk = NULL, n, correl)
}
\arguments{
\item{M}{A numeric vector of length 2 specifying the means for each group.}

\item{S}{A numeric vector of length 2 specifying the standard deviations for
each group.}

\item{Sk}{A numeric vector of length 2 specifying the skewness parameters for
each group, or NULL for no skewness. If NULL, normal distributions
will be used.}

\item{n}{A numeric vector of length 2 specifying the sample sizes for each
group.}

\item{correl}{A single numeric value specifying the correlation between the
two groups.}
}
\value{
A data frame with two columns:
\item{x}{The generated data values}
\item{grp}{The group labels (F1 or F2)}
}
\description{
This function generates correlated data for a two-group ANOVA (Analysis of
Variance) with the option for skewed distributions. It creates data for two
groups with specified means, standard deviations, skewness parameters, and
correlation.
}
\details{
All input vectors (M, S, and n) must have length 2, representing the
two groups. If Sk is provided, it must also have length 2. If Sk is
NULL, normal distributions are used for both groups. The function uses
MASS::mvrnorm for normal distributions and sn::rmsn for skew-normal
distributions.
}
\examples{
# Generate correlated data for 2 groups with no skewness
data1 <- generate_data_anova_cor(M = c(0, 2), S = c(1, 1), n = c(10, 10),
                                 correl = 0.5)

}
